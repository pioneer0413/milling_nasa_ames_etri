# Output Schema

Each normal experiment writes outputs under:

```text
experiments/executions/{experiment_id}/
```

The MVP writes the recommended minimum output set plus summaries, model artifacts, basic analysis tables, and figures when plotting libraries are available.
